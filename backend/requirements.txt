# ============================================================================
# Backend requirements.txt — Algorithmic Trading System (Fitschen + DhanHQ)
# Python 3.11+
# ============================================================================

# --- Web framework ---------------------------------------------------------
fastapi==0.110.0
uvicorn[standard]==0.27.1
gunicorn==21.2.0
python-multipart==0.0.9

# --- DB / ORM --------------------------------------------------------------
SQLAlchemy==2.0.27
psycopg2-binary==2.9.9
alembic==1.13.1

# --- Pydantic / validation -------------------------------------------------
pydantic==2.6.1
pydantic-settings==2.2.1
email-validator==2.1.0

# --- Auth / security -------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# --- Broker SDK ------------------------------------------------------------
# NOTE: 2.0.6 doesn't exist on PyPI (they jumped from 2.0.2 → 2.2.0).
# 2.2.0 is the latest stable; uses the same v2 API our DhanService wraps.
dhanhq==2.2.0

# --- Async / task queue ----------------------------------------------------
celery==5.3.6
redis==5.0.1
flower==2.0.1

# --- Numeric / indicators --------------------------------------------------
pandas==2.2.0
numpy==1.26.4
pandas-ta==0.3.14b0
scipy==1.12.0

# --- Date / time -----------------------------------------------------------
python-dateutil==2.8.2
pytz==2024.1

# --- HTTP client (for broker fallback) -------------------------------------
httpx==0.26.0
requests==2.31.0

# --- Logging / monitoring --------------------------------------------------
loguru==0.7.2
structlog==24.1.0

# --- Config ----------------------------------------------------------------
python-dotenv==1.0.1

# --- Testing ---------------------------------------------------------------
pytest==8.0.0
pytest-asyncio==0.23.5

# --- Utilities -------------------------------------------------------------
tenacity==8.2.3          # retry logic for broker calls
tabulate==0.9.0
